{
  "name": "Debian",

  "build": {
    "dockerfile": "Dockerfile"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cmake-tools"
      ]
    }
  },

  "postStartCommand": ". /.venv/bin/activate && cd /workspaces/amol-opt && pre-commit install"
}
